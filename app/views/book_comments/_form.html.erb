      <div>
        <%= form_with model: [book, book_comment], local: false do |f| %>
          <%= f.text_area :comment, size: "90x5", id: "comment_textarea" %>
          <%= f.submit "送信" %>
        <% end %>
      </div>